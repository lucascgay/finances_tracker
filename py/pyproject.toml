[project]
name = "finance-pdf-extract"
version = "0.1.0"
description = "Layout-aware PDF text extraction for the LocalFinance Tracker"
requires-python = ">=3.10"
dependencies = [
    "pdfplumber>=0.11.0",
]

[tool.uv]
package = false
